zenoh_camera:
  ros__parameters:
    image_topic:   /camera/image_raw
    zenoh_locator: tcp/127.0.0.1:7447  # 운용 시 GCS IP로 변경
    jpeg_quality:  80                  # 1~100 (낮을수록 대역폭 절약)
    max_fps:       30.0
